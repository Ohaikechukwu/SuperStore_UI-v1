"use client";

import FormSelect from "@/components/form-select";
import { FormEvent, useEffect, useState } from "react";
import { ExternalLink, Plus, RefreshCw } from "lucide-react";
import DashboardShell from "@/components/dashboard-shell";
import PermissionGate from "@/components/permission-gate";
import { api, ApiError } from "@/lib/api";
type Patient = { id: string; full_name: string; patient_number: string }; type Doc = { id: string; title: string; document_type: string; file_url: string; notes: string | null; created_at: string };
export default function Page() { const [patients, setPatients] = useState<Patient[]>([]); const [patientId, setPatientId] = useState(""); const [items, setItems] = useState<Doc[]>([]); const [open, setOpen] = useState(false); const [error, setError] = useState(""); async function loadPatients() { try { const list = await api.get<Patient[]>("/api/v1/clinical/patients"); setPatients(list); setPatientId((current) => current || list[0]?.id || ""); } catch (caught) { setError(caught instanceof ApiError ? caught.message : "Unable to load patients."); } } async function load() { if (!patientId) return; try { setItems(await api.get<Doc[]>(`/api/v1/clinical/patient-documents/${patientId}`)); } catch (caught) { setError(caught instanceof ApiError ? caught.message : "Unable to load documents."); } } useEffect(() => { void loadPatients(); }, []); useEffect(() => { void load(); }, [patientId]); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const d = new FormData(event.currentTarget); try { await api.post("/api/v1/clinical/patient-documents", { patient_id: patientId, title: d.get("title"), document_type: d.get("document_type"), file_url: d.get("file_url"), notes: d.get("notes") || null }); setOpen(false); await load(); } catch (caught) { setError(caught instanceof ApiError ? caught.message : "Unable to save document."); } }
  return <DashboardShell title="Patient documents" subtitle="Identity, consent, referral, and clinical files"><PermissionGate permission="hospital.patients.read" module="hospital"><main className="mx-auto max-w-[1050px] space-y-6"><header className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-teal-600">Patient registration</p><h1 className="mt-2 text-3xl font-bold">Documents</h1></div><div className="flex gap-2"><button onClick={() => void load()} className="rounded-xl border border-slate-200 bg-white p-3"><RefreshCw size={17} /></button><button onClick={() => setOpen(true)} disabled={!patientId} className="inline-flex items-center gap-2 rounded-xl bg-teal-600 px-4 py-2.5 text-sm font-bold text-white"><Plus size={16} /> Add document</button></div></header>{error && <p className="rounded-xl bg-rose-50 px-4 py-3 text-sm text-rose-700">{error}</p>}<FormSelect value={patientId} onChange={(event) => setPatientId(event.target.value)} className="w-full rounded-xl border border-slate-200 bg-white p-3 text-sm"><option value="">Select patient</option>{patients.map((p) => <option key={p.id} value={p.id}>{p.full_name} ({p.patient_number})</option>)}</FormSelect><section className="grid gap-4 sm:grid-cols-2">{items.map((item) => <article key={item.id} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-start justify-between gap-3"><div><p className="font-bold">{item.title}</p><p className="mt-1 text-xs uppercase text-slate-400">{item.document_type}</p></div><a href={item.file_url} target="_blank" rel="noreferrer" className="text-teal-700"><ExternalLink size={16} /></a></div>{item.notes && <p className="mt-3 text-sm text-slate-600">{item.notes}</p>}<p className="mt-3 text-xs text-slate-400">{new Date(item.created_at).toLocaleString()}</p></article>)}{!items.length && <p className="text-sm text-slate-500">No documents recorded for this patient.</p>}</section>{open && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><form onSubmit={(event) => void submit(event)} className="w-full max-w-md rounded-3xl bg-white p-6 shadow-xl"><h2 className="text-xl font-bold">Add patient document</h2><input name="title" required placeholder="Document title" className="mt-5 w-full rounded-xl border border-slate-200 p-3 text-sm" /><FormSelect name="document_type" className="mt-3 w-full rounded-xl border border-slate-200 p-3 text-sm"><option value="identity">Identity</option><option value="consent">Consent</option><option value="referral">Referral</option><option value="clinical">Clinical</option></FormSelect><input name="file_url" type="url" required placeholder="Secure file URL" className="mt-3 w-full rounded-xl border border-slate-200 p-3 text-sm" /><textarea name="notes" placeholder="Notes" className="mt-3 w-full rounded-xl border border-slate-200 p-3 text-sm" /><div className="mt-5 flex gap-2"><button type="button" onClick={() => setOpen(false)} className="flex-1 rounded-xl border border-slate-200 py-3 text-sm font-bold">Cancel</button><button className="flex-1 rounded-xl bg-teal-600 py-3 text-sm font-bold text-white">Save</button></div></form></div>}</main></PermissionGate></DashboardShell>; }
