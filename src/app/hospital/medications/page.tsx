"use client";

import FormSelect from "@/components/form-select";
import { FormEvent, useEffect, useState } from "react";
import { Plus, RefreshCw } from "lucide-react";
import DashboardShell from "@/components/dashboard-shell";
import PermissionGate from "@/components/permission-gate";
import { api, ApiError } from "@/lib/api";
type Patient = { id: string; full_name: string; patient_number: string }; type Admin = { id: string; patient_name: string; medication_name: string; dose: string; scheduled_at: string; status: string; notes: string | null };
export default function Page() { const [patients, setPatients] = useState<Patient[]>([]); const [items, setItems] = useState<Admin[]>([]); const [open, setOpen] = useState(false); const [error, setError] = useState(""); const [notice, setNotice] = useState("");
  async function load() { try { const [p, a] = await Promise.all([api.get<Patient[]>("/api/v1/clinical/patients"), api.get<Admin[]>("/api/v1/hospital/medications/administrations")]); setPatients(p); setItems(a); } catch (caught) { setError(caught instanceof ApiError ? caught.message : "Unable to load medication rounds."); } }
  useEffect(() => { void load(); }, []);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const d = new FormData(event.currentTarget); try { await api.post("/api/v1/hospital/medications/administrations", { patient_id: d.get("patient_id"), medication_name: d.get("medication_name"), dose: d.get("dose"), scheduled_at: new Date(String(d.get("scheduled_at"))).toISOString(), notes: d.get("notes") || null }); setOpen(false); setNotice("Medication dose scheduled."); await load(); } catch (caught) { setError(caught instanceof ApiError ? caught.message : "Unable to schedule dose."); } }
  async function update(item: Admin, status: string) { try { await api.post(`/api/v1/hospital/medications/administrations/${item.id}/status`, { status }); await load(); } catch (caught) { setError(caught instanceof ApiError ? caught.message : "Unable to update dose."); } }
  return <DashboardShell title="Medication administration" subtitle="Inpatient medication rounds and dose tracking"><PermissionGate permission="hospital.nursing.document" module="hospital"><main className="mx-auto max-w-[1100px] space-y-6"><header className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.18em] text-teal-600">Nursing care</p><h1 className="mt-2 text-3xl font-bold">Medication administration</h1></div><div className="flex gap-2"><button onClick={() => void load()} className="rounded-xl border border-slate-200 bg-white p-3"><RefreshCw size={17} /></button><button onClick={() => setOpen(true)} className="inline-flex items-center gap-2 rounded-xl bg-teal-600 px-4 py-2.5 text-sm font-bold text-white"><Plus size={16} /> Schedule dose</button></div></header>{error && <p className="rounded-xl bg-rose-50 px-4 py-3 text-sm text-rose-700">{error}</p>}{notice && <p className="rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{notice}</p>}<section className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm">{!items.length ? <p className="p-10 text-center text-sm text-slate-500">No medication doses scheduled.</p> : <div className="divide-y divide-slate-100">{items.map((item) => <article key={item.id} className="grid gap-3 p-5 sm:grid-cols-[1fr_1fr_auto] sm:items-center"><div><p className="font-bold">{item.patient_name}</p><p className="text-sm text-slate-600">{item.medication_name} · {item.dose}</p></div><p className="text-sm text-slate-500">{new Date(item.scheduled_at).toLocaleString()}</p><FormSelect value={item.status} onChange={(event) => void update(item, event.target.value)} className="rounded-xl border border-slate-200 px-3 py-2 text-xs font-bold"><option value="scheduled">Scheduled</option><option value="administered">Administered</option><option value="missed">Missed</option><option value="held">Held</option></FormSelect></article>)}</div>}</section>{open && <div className="fixed inset-0 z-50 grid place-items-center bg-slate-950/40 p-4"><form onSubmit={(event) => void submit(event)} className="w-full max-w-md rounded-3xl bg-white p-6 shadow-xl"><h2 className="text-xl font-bold">Schedule medication dose</h2><FormSelect name="patient_id" required className="mt-5 w-full rounded-xl border border-slate-200 p-3 text-sm"><option value="">Select patient</option>{patients.map((p) => <option key={p.id} value={p.id}>{p.full_name} ({p.patient_number})</option>)}</FormSelect><input name="medication_name" required placeholder="Medication name" className="mt-3 w-full rounded-xl border border-slate-200 p-3 text-sm" /><input name="dose" required placeholder="Dose (e.g. 500 mg)" className="mt-3 w-full rounded-xl border border-slate-200 p-3 text-sm" /><input name="scheduled_at" type="datetime-local" required className="mt-3 w-full rounded-xl border border-slate-200 p-3 text-sm" /><div className="mt-5 flex gap-2"><button type="button" onClick={() => setOpen(false)} className="flex-1 rounded-xl border border-slate-200 py-3 text-sm font-bold">Cancel</button><button className="flex-1 rounded-xl bg-teal-600 py-3 text-sm font-bold text-white">Schedule</button></div></form></div>}</main></PermissionGate></DashboardShell>; }
