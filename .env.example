NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_TENANT_KEY=

# Vercel + ngrok: keep browser requests same-origin so the HttpOnly refresh
# cookie is sent to /edge-api/api/v1/auth. EDGE_API_UPSTREAM is server-only.
# NEXT_PUBLIC_API_URL=/edge-api
# EDGE_API_UPSTREAM=https://your-stable-tunnel.ngrok-free.app
